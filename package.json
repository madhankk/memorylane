{
  "name": "memorylane",
  "private": true,
  "version": "0.1.0",
  "description": "Self-hosted photo and video browser for rediscovering existing photo archives.",
  "license": "MIT",
  "workspaces": [
    "shared",
    "server",
    "client",
    "desktop"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "build": "npm run build --workspace=shared && npm run build --workspace=client && npm run build --workspace=server",
    "start": "npm run start --workspace=server",
    "reset-password": "npm run reset-password --workspace=server --",
    "test": "npm run test --workspace=server",
    "typecheck": "npm run typecheck --workspace=shared && npm run typecheck --workspace=server && npm run typecheck --workspace=client"
  }
}
