node_modules/
dist/
build/
server/public/
*.log
.DS_Store
Thumbs.db

# local dev data (sqlite db, thumbnails, logs) - never commit generated state
server/data/
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# env
.env
.env.local

# desktop tray app - runtime/ is the assembled node binary + server files
# (see desktop/scripts/prepare-runtime.mjs), release/ is electron-forge's
# `make`/`package` output (see desktop/forge.config.ts's outDir)
desktop/runtime/
desktop/release/
desktop/out/
